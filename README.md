# develop-utils
develop-utils is a small CLI for generating repeatable repository setup files — licenses, authorship docs, line-ending policy, and semantic-release workflows — with git-derived defaults and dry-run review before writing.
